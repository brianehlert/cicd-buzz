sudo: required

language: python
python:
  - "3.6"

script:
  - python -m pytest -v

services:
  - docker

before_install:
  - docker build -t cicd-buzz .
  - docker run -p 5000:5000 --rm -it cicd-buzz
  
deploy:
   provider: script
   script: bash docker_push.sh
   on:
     branch: master
